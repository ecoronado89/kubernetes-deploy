apiVersion: v1
kind: Service
metadata:
  name: selenium-hub
  labels:
    app: automation-framework
spec:
  ports:
  - port: 4444 
    targetPort: 4444 
    name: port0
  selector:
    app: automation-framework
  type: NodePort
  sessionAffinity: None